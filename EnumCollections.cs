using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
    public class EnumCollections
    {
        public enum OpenWay
        {
            New = 0,
            Open = 1,
            OpenReadOnly = 2
        };
        public enum Save
        {
            DoNotSave = 0,
            Save = 1,

        };

    }
